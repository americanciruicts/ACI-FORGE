# ==============================================================================
# ACI FORGE Backend Environment Configuration
# ==============================================================================
# SECURITY WARNING: Never commit the actual .env file to version control!
# This is a template file. Copy it to .env and fill in your actual values.
# ==============================================================================

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# PostgreSQL database connection string
# Format: postgresql://username:password@host:port/database_name
# SECURITY: Use strong passwords and restrict database user permissions
DATABASE_URL=postgresql://aci_dashboard_user:CHANGE_THIS_PASSWORD@aci-database:5432/aci_forge

# ==============================================================================
# JWT AUTHENTICATION
# ==============================================================================
# CRITICAL: These keys MUST be changed and kept secret in production!
# Generate secure keys with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Keys must be at least 32 characters long
JWT_SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARS
JWT_REFRESH_SECRET_KEY=CHANGE_THIS_TO_A_DIFFERENT_SECURE_RANDOM_STRING

# Token expiration times
# Access token: How often the token refreshes (recommended: 15-60 minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh token: Maximum session duration before forced re-login
# Examples:
#   - 10 hours: REFRESH_TOKEN_EXPIRE_DAYS=0.4167
#   - 8 hours:  REFRESH_TOKEN_EXPIRE_DAYS=0.3333
#   - 12 hours: REFRESH_TOKEN_EXPIRE_DAYS=0.5
#   - 1 day:    REFRESH_TOKEN_EXPIRE_DAYS=1
#   - 7 days:   REFRESH_TOKEN_EXPIRE_DAYS=7
REFRESH_TOKEN_EXPIRE_DAYS=0.4167

# ==============================================================================
# ENVIRONMENT
# ==============================================================================
# Environment: development, staging, or production
# In production, OpenAPI docs and debug features are automatically disabled
ENVIRONMENT=development

# Debug mode (only enable in development)
DEBUG=true

# ==============================================================================
# CORS CONFIGURATION
# ==============================================================================
# Comma-separated list of allowed origins for CORS
# SECURITY: In production, specify exact origins - never use wildcard (*)
# Examples:
#   - Development: http://localhost:3000,http://localhost:2005
#   - Production: https://dashboard.example.com,https://app.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:2005,http://192.168.1.95:2005

# ==============================================================================
# EMAIL CONFIGURATION (SMTP)
# ==============================================================================
# Leave SMTP_USERNAME and SMTP_PASSWORD empty to run in email simulation mode
# Email simulation mode will log emails instead of sending them

# SMTP server hostname (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_SERVER=

# SMTP port (usually 587 for TLS, 465 for SSL, 25 for unencrypted)
# SECURITY: Always use TLS (587) or SSL (465) in production
SMTP_PORT=587

# SMTP authentication credentials
SMTP_USERNAME=
SMTP_PASSWORD=

# From email address for outgoing emails
FROM_EMAIL=

# ==============================================================================
# FRONTEND CONFIGURATION
# ==============================================================================
# Frontend URL for generating links in emails
# Used for password reset links, login links, etc.
FRONTEND_URL=http://192.168.1.95:2005

# ==============================================================================
# SECURITY BEST PRACTICES
# ==============================================================================
# 1. NEVER commit .env files to version control
# 2. Use strong, unique passwords for all credentials
# 3. Rotate JWT secrets regularly (at least every 90 days)
# 4. Use environment-specific configurations (dev, staging, prod)
# 5. Enable HTTPS/TLS for all production environments
# 6. Restrict database user permissions to minimum required
# 7. Use secrets management tools (AWS Secrets Manager, HashiCorp Vault, etc.) in production
# 8. Regular security audits and dependency updates
# 9. Monitor logs for suspicious activity
# 10. Implement rate limiting and IP blocking for API endpoints

# ==============================================================================
# OPTIONAL: ADDITIONAL SECURITY SETTINGS
# ==============================================================================
# Bcrypt rounds for password hashing (higher = more secure but slower)
# BCRYPT_ROUNDS=12

# ==============================================================================
# DEPLOYMENT CHECKLIST
# ==============================================================================
# Before deploying to production:
# [ ] Change all default passwords and secrets
# [ ] Set ENVIRONMENT=production
# [ ] Set DEBUG=false
# [ ] Configure specific ALLOWED_ORIGINS (no wildcards)
# [ ] Enable HTTPS/TLS
# [ ] Configure email SMTP with TLS/SSL
# [ ] Set up database backups
# [ ] Configure logging and monitoring
# [ ] Review and test all security settings
# [ ] Enable firewall rules
# [ ] Set up rate limiting
